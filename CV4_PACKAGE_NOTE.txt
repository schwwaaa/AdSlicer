AdSlicer CV Pass 04 — Shadow Edit Planner

Baseline: accepted CV-3 unified CLI built from the user's authoritative current AdSlicer code.

This pass adds shadow-only edit interval planning and current-default legacy comparison.
It does not change production AdSlicer cuts or render media.

Stable test command:
  ./test-opencv.sh --input "/path/to/video.mp4"

Synthetic regression:
  ./test-opencv.sh --synthetic

Primary report:
  cv-test-output/<source>/OPENCV_TEST_REPORT.txt

CV-4 details:
  docs/development/CV4_SHADOW_EDIT_PLAN.md
